import 'package:flutter/material.dart';

class AppColor {
  static const Color primary = Colors.cyan;
  static Color secondary = Colors.white.withValues(alpha: 0.90);
}

class AppTextStyle {
  static const TextStyle headline = TextStyle(
    color: Colors.cyan,
    fontWeight: FontWeight.bold,
    fontSize: 23,
  );
}
